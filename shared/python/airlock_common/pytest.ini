[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    asyncio: marks tests as async
    requires_db: marks tests as requiring a database connection
    integration: marks tests as integration tests (require external services)

